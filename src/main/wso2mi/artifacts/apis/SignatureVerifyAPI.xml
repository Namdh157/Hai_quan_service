<?xml version="1.0" encoding="UTF-8"?>
<api context="/api/v1/signature" name="SignatureVerifyAPI" xmlns="http://ws.apache.org/ns/synapse">

    <!-- ================= UC-25 ================= -->
    <resource methods="POST" uri-template="/verify">

        <inSequence>

            <!-- 1. Extract Properties -->
            <property name="requestId" expression="//requestId/text()" scope="default"/>
            <property name="signedData" expression="//signedData/text()" scope="default"/>
            <property name="signature" expression="//signature/text()" scope="default"/>
            <property name="signTime" expression="//signTime/text()" scope="default"/>
            <property name="authToken" expression="get-property('BackendAuthToken')" scope="default"/>
            <property name="backendPath" value="/api/v1/signature/verify" scope="default"/>

            <!-- 2. Validate Compulsory Fields -->
            <filter xpath="string-length(get-property('requestId')) = 0 or string-length(get-property('signedData')) = 0 or string-length(get-property('signature')) = 0 or string-length(get-property('signTime')) = 0 or count(//certChain/cert) = 0">
                <then>
                    <property name="HTTP_SC" value="400" scope="axis2"/>
                    <payloadFactory media-type="xml">
                        <format>
                            <error>
                                <code>SIG_001</code>
                                <message>Sai định dạng chữ ký - Missing mandatory fields</message>
                            </error>
                        </format>
                        <args/>
                    </payloadFactory>
                    <respond/>
                </then>
                <else>
                    <!-- 3. Validation Passed -> Log Details -->
                    <log level="custom">
                        <property name="UseCase" value="UC25-SignatureVerify"/>
                        <property name="requestId" expression="get-property('requestId')"/>
                        <property name="backendPath" expression="get-property('backendPath')"/>
                    </log>

                    <!-- 4. Backend Call -->
                    <header name="Authorization" expression="get-property('authToken')" scope="transport"/>
                    <http.post configKey="BackendConnection">
                        <relativePath>{$ctx:backendPath}</relativePath>
                    </http.post>
                    <respond/>
                </else>
            </filter>

        </inSequence>

    </resource>

</api>
